apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: books
type: Opaque
stringData:
  # For demo purposes only. Replace in real deployments and store in an external secret manager.
  POSTGRES_PASSWORD: devpassword
  DB_PASSWORD: devpassword
